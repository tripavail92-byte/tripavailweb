'use client';

export default function DeparturesPage() {
  return (
    <div className="space-y-3">
      <h1 className="text-2xl font-semibold">Departures</h1>
      <p className="text-sm text-neutral-700">
        Departure management will use tour package ids to attach departure dates once backend endpoints are available. For
        now, manage departures through Step 3 on the Tours page.
      </p>
    </div>
  );
}
